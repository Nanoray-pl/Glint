dependencies {
	compileOnly project(':core')
	compileOnly project(':command')
	compileOnly project(':owner')
	compileOnly project(':store')
}

test {
	useJUnitPlatform()

	dependsOn 'cleanTest'

	testLogging {
		events "passed", "skipped", "failed"
	}
}