dependencies {
	compileOnly project(':core')
	api 'io.mikael:urlbuilder:2.0.9'
}

test {
	useJUnitPlatform()

	dependsOn 'cleanTest'

	testLogging {
		events "passed", "skipped", "failed"
	}
}